# game-ai-news-bot 작업 규칙

- 목적은 게임 개발자가 실제로 판단에 쓸 수 있는 AI 동향 브리핑이다. 일반 AI 뉴스나 투자 홍보량을 늘리는 방향으로 확장하지 않는다.
- RSS/API를 우선하고 HTML 목록 수집은 RSS가 없을 때만 사용한다. 로그인·페이월·캡차를 우회하지 않는다.
- 기사 본문을 재배포하지 않는다. 제목, 짧은 발췌, 자체 요약, 원문 링크만 전송한다.
- 회사 공식 블로그는 `perspective: vendor`로 표시하고 독립 분석/전문 보도보다 낮은 가중치를 유지한다.
- 새 소스를 추가하거나 파서를 바꾸면 `python -m unittest discover -s tests -v`를 통과시키고 실제 `--dry-run` 결과를 확인한다.
- `.env`와 텔레그램·Gemini 토큰은 절대 커밋하지 않는다.
- 커밋 메시지는 한글로 작성하고 푸시는 사용자 승인 후 수행한다.

## 운영 채널 게시 규칙 (2026-09-06)

- `@game_ai_news_search`는 실제 구독자가 있는 운영 채널이다.
- 게시 전 미리보기는 기본적으로 `--dry-run`으로 만들고 현재 대화에 보여준다.
- 운영 채널 검증 발송은 사용자가 명시적으로 요청한 경우에만, 요청한 건수만 전송한다. 채널 고정·삭제도 별도 요청 없이 하지 않는다.
- 운영 채널에 보내는 글에는 `테스트 메시지입니다`, `테스트 발송`, `미리보기`, `DRY-RUN` 같은 검증 안내·개발용 문구를 넣지 않는다. 검증 로그는 콘솔/Actions에만 남긴다.
- 검증 목적이라도 실제 운영용 기사라면 정상 발송 경로로 읽음 기록·일일 한도에 반영해 예약 발송에서 중복되지 않게 한다. 단건 기사 요청에 자매 채널 광고를 추가하지 않는다.
- GeekNews는 공식 RSS를 사용한다. 한국어 짧은 소개와 긱뉴스 링크를 제공하고, 요약 전문·댓글을 그대로 재배포하거나 차단된 상세 페이지를 우회하지 않는다.
- GeekNews는 게임 AI와 게임 제작에 활용할 수 있는 AI 개발 도구에 한해 하루 최대 2건 선별한다. 일반 AI 투자·잡담·하드웨어 소식은 제외한다.
