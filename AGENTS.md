# game-ai-news-bot 작업 규칙

- 목적은 게임 개발자가 실제로 판단에 쓸 수 있는 AI 동향 브리핑이다. 일반 AI 뉴스나 투자 홍보량을 늘리는 방향으로 확장하지 않는다.
- RSS/API를 우선하고 HTML 목록 수집은 RSS가 없을 때만 사용한다. 로그인·페이월·캡차를 우회하지 않는다.
- 기사 본문을 재배포하지 않는다. 제목, 짧은 발췌, 자체 요약, 원문 링크만 전송한다.
- 회사 공식 블로그는 `perspective: vendor`로 표시하고 독립 분석/전문 보도보다 낮은 가중치를 유지한다.
- 새 소스를 추가하거나 파서를 바꾸면 `python -m unittest discover -s tests -v`를 통과시키고 실제 `--dry-run` 결과를 확인한다.
- `.env`와 텔레그램·Gemini 토큰은 절대 커밋하지 않는다.
- 커밋 메시지는 한글로 작성하고 푸시는 사용자 승인 후 수행한다.
